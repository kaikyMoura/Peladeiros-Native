import {StyleSheet} from "react-native";

export const styles = StyleSheet.create({
    container: {
        width: '100%',
        height: 'auto',
    },
    presStyle: {
        width: '100%',
        height: 'auto',
        padding: 8,
        borderRadius: 4,
        display: 'flex',
        alignItems: 'center',
        backgroundColor: '#dddddd',
    },
    imageStyle: {
        width: '100%',
        height: 200,
        marginTop: 16,
    },
    textStyle: {
      fontSize: 12,
      color: '#000000',
    }
});
